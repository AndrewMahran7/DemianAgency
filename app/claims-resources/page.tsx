import { RouteShell } from "@/components/demian/route-shell";
export default function ResourcesPage() { return <RouteShell eyebrow="Claims & resources" title="Clear next steps when you need them." description="This resource hub is ready for verified claims contacts, carrier links, document guidance, and policy resources. No unconfirmed instructions are shown." />; }
